import { createStyles } from "@material-ui/styles";

export default () =>
  createStyles({
    root: {
      fontWeight: "bold",
      fontSize: "xx-large",
      margin: "30px 0",
      color: "#1D84B5"
    }
  });
